opencv-python>=4.8.0
mediapipe>=0.10.0
pygame>=2.5.0
pyyaml>=6.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
xgboost>=1.7.0   # optional for training
joblib>=1.3.0
# torch and torchvision (Naruto image classifier, transfer learning):
# a plain `pip install -r requirements.txt` will pull a version, but for a
# CPU-only machine prefer:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.15.0